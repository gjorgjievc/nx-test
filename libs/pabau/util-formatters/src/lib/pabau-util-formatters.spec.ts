import { pabauUtilFormatters } from './pabau-util-formatters';

describe('pabauUtilFormatters', () => {
  it('should work', () => {
    expect(pabauUtilFormatters()).toEqual('pabau-util-formatters');
  });
});
